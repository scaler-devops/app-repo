apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gitops-demo
  template:
    metadata:
      labels:
        app: gitops-demo
    spec:
      containers:
        - name: app
          image: rashmikakashyap/gitops-demo-app:783d6e9
          ports:
            - containerPort: 5000
          env:
            - name: APP_VERSION
              value: "latest"

